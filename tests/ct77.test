package require tcltest
source constcl.tcl
namespace import -force tcltest::*


test control-1.0 {try procedure?)} -body {
    pew {(procedure? car)}
    pew {(procedure? 'car)}
    pew {(procedure? (lambda (x) (* x x)))}
} -output "#t\n#f\n#t\n"



cleanupTests
return
