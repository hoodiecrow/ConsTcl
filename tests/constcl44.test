
package require tcltest
namespace import -force tcltest::*
source ../constcl.tcl

test setup-1.0 {equivalence of instance and alias} -body {
        expr {[pe "'()"] == ${#NIL}}
} -result 1

test setup-1.1 {equivalence of instance and alias} -body {
        expr {[::constcl::NIL new] == ${#NIL}}
} -result 1


cleanupTests
return
