package require tcltest
source constcl.tcl
namespace import -force tcltest::*


test strings-17.0 {try string-ci>=?} -body {
    pew {(string-ci>=? "bar" "Car")}
    pew {(string-ci>=? "bar" "Bar")}
    pew {(string-ci>=? "bar" "Aar")}
} -output "#f\n#t\n#t\n"



cleanupTests
return
